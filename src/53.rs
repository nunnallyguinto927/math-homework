fn main() {
    // Define some constants and variables
    let x = 5;
    let y = 10;

    // Perform some arithmetic operations
    let result = x + y * 2;

    // Print the result
    println!("The result is: {}", result);
}
